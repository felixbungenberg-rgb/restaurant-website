---
layout: base.njk
title: "Speisekarte"
permalink: /speisekarte/
---

<style>
  .menu-page { padding: 4rem 2rem 5rem; background: var(--white); }

  .menu-section { max-width: 820px; margin: 0 auto 3.5rem; }

  .menu-section-title {
    font-family: 'Playfair Display', serif;
    font-size: 1.4rem; font-weight: 600;
    color: var(--black);
    padding-bottom: 0.75rem;
    border-bottom: 1px solid rgba(167,195,195,0.35);
    margin-bottom: 0.25rem;
  }

  .menu-item {
    display: flex; justify-content: space-between;
    align-items: flex-start; gap: 1.5rem;
    padding: 1.1rem 0;
    border-bottom: 0.5px solid rgba(167,195,195,0.25);
  }

  .menu-item:last-child { border-bottom: none; }

  .menu-item-name {
    font-family: 'Playfair Display', serif;
    font-size: 1rem; margin-bottom: 0.2rem;
  }

  .menu-item-desc { font-size: 0.82rem; color: var(--muted); margin-bottom: 0.4rem; line-height: 1.5; }

  .menu-item-price {
    font-family: 'Playfair Display', serif;
    font-size: 0.95rem; color: var(--black);
    white-space: nowrap; padding-top: 0.15rem; flex-shrink: 0;
  }

  .menu-note {
    max-width: 820px; margin: 0 auto;
    font-size: 0.78rem; color: var(--muted);
    border-top: 0.5px solid rgba(167,195,195,0.35);
    padding-top: 1.5rem; line-height: 1.7;
  }

  .tapas-note {
    max-width: 820px; margin: 0 auto 3rem;
    background: rgba(167,195,195,0.1);
    border: 0.5px solid rgba(167,195,195,0.4);
    border-radius: 4px;
    padding: 1.25rem 1.5rem;
    font-size: 0.85rem; color: var(--muted); line-height: 1.65;
  }
</style>

<div class="page-hero">
  <span class="eyebrow">Strandmuschel Rantum · Sylt</span>
  <h1 class="section-title">Speisekarte</h1>
  <p style="font-size:0.9rem;color:var(--muted);margin-top:0.5rem">Täglich frisch — regional, mediterran, mit Herzblut gemacht.</p>
</div>

<div class="menu-page">
  <div class="tapas-note">
    <strong>Tapas ab 16:30 Uhr</strong> — Unsere hausgemachten Tapas sind täglich ab 16:30 Uhr erhältlich. Zur freien Wahl — nehmt euch, was euch schmeckt.
  </div>

  {% for section in menu.sections %}
  <div class="menu-section">
    <h2 class="menu-section-title">{{ section.name }}</h2>
    {% for item in section.items %}
    <div class="menu-item">
      <div>
        <h3 class="menu-item-name">{{ item.name }}</h3>
        <p class="menu-item-desc">{{ item.description }}</p>
        <div>
          {% for tag in item.tags %}
            {% if tag != 'featured' %}<span class="tag">{{ tag }}</span>{% endif %}
          {% endfor %}
        </div>
      </div>
      <div class="menu-item-price">{{ item.price }}</div>
    </div>
    {% endfor %}
  </div>
  {% endfor %}

  <p class="menu-note">
    (v) vegetarisch &nbsp;·&nbsp; (vegan) vegan &nbsp;·&nbsp;
    Bitte teilt unserem Team Allergien und Unverträglichkeiten mit. Alle Preise inkl. MwSt.
    Die Speisekarte kann saisonal variieren.
  </p>
</div>
